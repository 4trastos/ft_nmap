#include "ft_nmap.h"

void    init_struct(struct config *conf)
{
    conf->argv = NULL;
    conf->argc = 0;
    conf->is_valid = false;
    conf->show_help = false;
}

int ft_parser(struct config *conf, char **argv)
{

    return (0);
}